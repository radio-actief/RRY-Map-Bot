/**
 * USB serial UI constants (transport in repeater-serial.js).
 */
(function (App) {
  "use strict";
  App.serialUi = {
    MAX_LINE_LEN: 151,
    DEFAULT_BAUD: 115200,
  };
})(window.ConfiguratorApp);
